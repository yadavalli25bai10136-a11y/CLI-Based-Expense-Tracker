# CLI-Based-Expense-Tracker
A command-line based expense tracker built using Python that allows users to record daily expenses, view transaction history, and generate category-wise summaries. The project uses JSON for data storage and is designed to run entirely through the terminal with minimal setup.
